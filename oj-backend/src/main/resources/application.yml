server:
  port: 8080

spring:
  #?????
  datasource:
    url: jdbc:mysql://localhost:3306/onlinejudge?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 12345
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  # mapper XML ??????
  mapper-locations: classpath:mapper/*.xml
  configuration:
    # ???????? ? ????????user_name ? userName?
    map-underscore-to-camel-case: true
    # ?????? SQL ???????
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
