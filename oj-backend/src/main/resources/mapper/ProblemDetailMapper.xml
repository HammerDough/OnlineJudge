<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hammerdough.mapper.ProblemDetailMapper">

    <select id="selectDetailByProblemId" resultType="com.hammerdough.entity.ProblemDetail">
        SELECT
            description,
            input_description,
            output_description,
            sample_input,
            sample_output,
            hint,
            time_limit,
            memory_limit
        FROM problem_detail
        WHERE problem_id = #{problemId}
    </select>

</mapper>